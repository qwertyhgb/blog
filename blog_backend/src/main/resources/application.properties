# ====================
# Spring Boot 主配置文件
# ====================

# 激活的配置文件：dev(开发环境) / prod(生产环境)
# 通过环境变量SPRING_PROFILES_ACTIVE设置，如果没有设置则默认使用dev开发环境
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# 应用名称，在Spring Cloud等微服务场景中使用
spring.application.name=blog

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog.mapper.CommentMapper">
    
    <resultMap id="BaseResultMap" type="com.example.blog.entity.Comment">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="parent_id" property="parentId"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <resultMap id="DetailResultMap" type="com.example.blog.entity.Comment" extends="BaseResultMap">
        <association property="user" javaType="com.example.blog.entity.User">
            <id column="user_id" property="id"/>
            <result column="user_username" property="username"/>
            <result column="user_nickname" property="nickname"/>
            <result column="user_avatar" property="avatar"/>
        </association>
    </resultMap>
    
    <select id="findByPostId" resultMap="DetailResultMap">
        SELECT c.*, 
               u.username as user_username, u.nickname as user_nickname, u.avatar as user_avatar
        FROM comment c
        JOIN user u ON c.user_id = u.id
        WHERE c.post_id = #{postId} AND c.status = 1 AND c.is_deleted = 0
        ORDER BY c.create_time ASC
    </select>
    
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM comment WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM comment WHERE id = #{id}
    </select>
    
    <select id="findByParentId" resultMap="BaseResultMap">
        SELECT * FROM comment WHERE parent_id = #{parentId} ORDER BY create_time ASC
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (post_id, user_id, parent_id, content, status)
        VALUES (#{postId}, #{userId}, #{parentId}, #{content}, #{status})
    </insert>
    
    <update id="update">
        UPDATE comment
        <set>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM comment WHERE id = #{id}
    </delete>
    
    <!-- 管理后台：分页查询所有评论 -->
    <select id="findAllWithPage" resultMap="DetailResultMap">
        SELECT c.*, 
               u.username as user_username, u.nickname as user_nickname, u.avatar as user_avatar
        FROM comment c
        JOIN user u ON c.user_id = u.id
        WHERE c.is_deleted = 0
        <if test="status != null">
            AND c.status = #{status}
        </if>
        ORDER BY c.create_time DESC
        LIMIT #{offset}, #{size}
    </select>
    
    <select id="countAll" resultType="java.lang.Long">
        SELECT COUNT(*) FROM comment c
        WHERE c.is_deleted = 0
        <if test="status != null">
            AND c.status = #{status}
        </if>
    </select>
    
</mapper>